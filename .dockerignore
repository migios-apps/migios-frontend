**/node_modules
**/dist
**/.git
**/.turbo
**/.DS_Store
**/*.log
**/coverage
**/.vercel
**/.netlify

# Env comes from build args only, so the image build stays deterministic and no
# development credential is silently baked into a production bundle.
.env
.env.*
!.env.example

.buildVersion.cache.json
.vscode
.idea
.cursor
.claude
